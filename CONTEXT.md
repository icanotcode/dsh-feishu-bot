# 飞书持续会话

飞书用户拥有各自的当前对话与可检索历史。上下文轮换不删除历史。

## 术语

**用户身份**：经飞书验证的租户与用户标识所指向的人；显示名称仅用于辨识，改名不会改变身份。

**可用范围**：由飞书应用管理方设置的应用使用人群；它与用户在对话中选择的姓名无关。

**确认姓名**：用户亲自提供并确认的称呼，用于用户资料与会话名称；相同姓名不表示相同用户。

**姓名确认**：用户开始普通对话前完成称呼提供与确认的过程；尚未完成时，对话仅用于确认姓名。

**聊天**：用户与机器人的一次私聊或一个群聊环境。同一用户在不同聊天中的上下文与可检索历史互不混用。

**当前会话**：用户在一个聊天中持续进行的对话。普通后续消息延续它，显式新建或日轮换后改用新的会话。

**日轮换**：在指定时区和时刻结束当天当前上下文的操作。正在执行的任务先完成，历史记录保留。

**历史记录**：带有发生时间的用户消息、智能体答复和用户保存的记忆；它不依赖当前上下文是否仍然打开。

**历史删除**：使记录不再出现在智能体常规查询中的操作。它不表示已从所有备份及 Harness 日志中物理擦除。
