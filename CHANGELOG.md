# 更新日志

## Unreleased

- 会话策略改为每位飞书用户同一时间最多一个当前可见的 Harness 会话；`/new`、默认凌晨 4 点日切及跨私聊/群聊切换都会归档旧会话，运行任务先完成，下一条消息使用新上下文。
- 归档保留 SQLite 历史、Harness 归档日志和工作文件；历史工具继续按当前用户与聊天隔离，重启时补归档已关闭但遗留在当前列表的记录。
- 旧数据库缺少身份资料时，在首次通过验证的飞书消息中自动补齐；此后即使无人发消息，也可定时切换上下文。

- 新增飞书文件、图片、视频、音频和图文（`post`）消息接收；每条最多 10 个资源、合计 100 MiB，保存到个人 `incoming/...` 并记录原名、时间与历史相对路径。未确认姓名不下载附件；`/new` 和每日切换保留文件。
- 新增 `feishu_send_file`：仅回复当前已领取的飞书消息，发送个人目录中的文件/图片/MP4 视频/OPUS 音频，可选视频封面及毫秒时长；图片上限 10 MiB，其他类型 30 MiB，无任意收件人参数。其他媒体格式可作为普通文件发送。
- 新增 `feishu_read_image` 读取个人目录历史图片，要求当前模型明确支持图片输入；纯文本模型只保存图片文件。工作区写入工具可生成 TXT/Markdown/CSV 后发回，不增加任意 Shell、模型、OCR、转码、视频分析、转写或通用文档解析。
- 附件能力仅更新插件，沿用唯一 Plugin list 入口，无需修改 Harness 核心；补充资源 API 权限、发布步骤和附件排错说明。

- 新增 Linux、Windows、macOS 原生 ngrok/cloudflared 一键启动、停止与隧道守护开关，跟随当前 Harness 端口，失败退避重试，仅管理插件自身进程。
- Cloudflare 支持临时隧道及本地管理的固定命名隧道；固定模式使用临时配置副本，不改原始文件，临时域名变化仍需更新飞书回调。ngrok 支持保留 Traffic Policy 与凭据。

- 删除 Harness 中的飞书工作区记录后，下一条普通飞书消息会重建工作区注册并挂回原会话；支持运行中复用及重启后恢复，保留上下文与历史。磁盘文件删除不属于此恢复范围。

- 修复 Harness 手动归档后飞书仍复用隐藏会话的问题：下一条普通消息会开启可见的新会话，旧历史保留；已有任务完成后再切换，`/status` 会提示归档状态。

- 取消插件中的 `open_id` 与用户名授权名单入口，接入范围交由飞书应用后台管理。旧配置中的个人权限覆盖继续保留，原名单不再作为准入限制。
- 新增首次对话姓名确认：用户提供姓名后须回复“确认”，才能发起普通任务或使用会话命令；确认前只处理姓名确认，之前的任务需确认后重新发送。旧名单中的姓名不会自动完成确认。
- 已确认姓名持久保存在用户资料中，用于会话标题；重启、`/new` 和每日上下文切换保留姓名。数据库与工作目录仍按稳定身份隔离，同名用户不合并数据。

- 飞书配置统一收进 **设置 → 插件 → Plugin list** 的 `feishu-bot` 卡片；搜索 `feishu` 并展开全局插件卡片即可使用。移除独立飞书标签页和侧栏入口。
- 当前通过列表卡片的 `data-*` 属性适配配置表单，并非 Harness 官方原生卡片配置扩展；补充入口与升级兼容排查说明。

## 0.2.0

- 同一租户、授权用户与聊天默认复用 Harness 会话；按管理员配置的显示名称命名。不同用户、群聊和私聊分别维护上下文。
- 新增 `/new`、`/whoami`、`/help`、`/status`。`/new` 不取消旧任务，新任务等待旧任务完成；未授权用户可用 `/whoami` 查询自己的身份。
- 新增默认拒绝的用户授权名单，每位用户使用独立工作目录及 SQLite 数据库。空名单拒绝所有普通任务；升级后需由本机管理员添加 `open_id` 和显示名称。
- 历史保存消息时间戳；Agent 可搜索、读取、创建笔记、修改、软删除当前用户在当前聊天的记录。历史编辑不会追溯修改 Harness 日志、飞书消息或模型已加载上下文，刷新上下文需另发 `/new`。
- 默认每天 Asia/Macau 凌晨 4 点切换上下文；任务运行中等待完成，历史保留。可配置小时和时区；不提供任意 cron 或通用定时任务执行器。
- 收紧远程权限：只允许 `read-only` / `workspace-write`，仅开放受限工作区文件工具及历史工具；不开放任意 Shell、通用 MCP 或跨用户飞书 API。可为每个授权用户单独指定只读或工作区写入，省略时继承全局预设。旧版完全访问配置须先修改，旧会话不会自动合并或导入历史库。
- Node.js 最低版本提高到 **22.13**，使用内置 `node:sqlite`。旧版界面截图不包含新增设置。

0.2.0 的真实飞书多用户收发、隧道连通和三平台完整行为仍需在目标环境验收；自动测试与本地检查不代表这些场景已全部通过。

## 0.1.4

- 设置页显示实际 Harness 监听端口，并据此生成 ngrok 启动命令；说明更换端口需要重启 Harness 并同步隧道目标。
- 修复 Windows 下误把 npm shell shim 当作可执行入口的问题，补充 Windows npm 缓存路径查找。
- 自动测试矩阵扩展至 Linux、Windows、macOS 与 Node.js 22/24；补充跨平台使用说明，真实飞书收发仍需在目标环境验收。

## 0.1.3

- Webhook 设置新增公网接入方式选择：ngrok、Cloudflare Tunnel、自定义公网地址。保留 ngrok 默认行为；Cloudflare 和自定义方式使用手动填写的 HTTPS 根地址，不回退至 ngrok，不将配置成功当作公网连通。
- 新增 `npm run start:cloudflare`，支持 Quick Tunnel、启动现有命名隧道以及后台运行。Quick Tunnel 域名需手动同步到插件与飞书回调配置；长连接仍无需任何隧道。
- 接受有效飞书文本任务后自动添加 `Typing` 敲键盘表情回应，完成、错误、取消或卸载时尝试移除自己的表情。需开通 `im:message.reactions:write_only` 并完成权限发布；状态更新失败不影响任务与文本回复。
- 补充 Cloudflare 配置、表情权限和排错文档。添加工作状态表情不代表支持接收用户 reaction 事件。

真实飞书消息与 Cloudflare 公网链路需在使用者环境完成端到端验证。

## 0.1.2

- 提供 Harness Web profile 飞书插件设置页、Webhook / 长连接切换、应用凭据管理和连接状态显示。
- 接收飞书文本消息并创建独立 Harness 会话，完成后回复最终文本。
- 提供源码安装、ngrok 启动脚本、中英文项目说明与接入指南。
